node_modules
dist
.vite
.DS_Store

# Secrets — the ElevenLabs key for scripts/gen-voices.mjs lives in a local .env,
# never committed. The rendered MP3s under public/voices/ ARE committed.
.env
.env.*
!.env.example

# Local-only OSS-readiness audit. It documents the pre-publication review and is
# deliberately never published; ignoring it means a stray `git add -A` can't
# commit it into what is now a public repo.
OSS-AUDIT.md
